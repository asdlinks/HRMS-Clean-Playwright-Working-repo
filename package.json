{
  "name": "hrms-e2e-clean",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright UI suite converted to standard @playwright/test house style.",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1",
    "@types/node": "^26.1.1",
    "dotenv": "^17.4.2",
    "typescript": "^7.0.2"
  }
}
